# Cocos MCP Kit 项目开发约束

## 底座与许可

- 本项目基于 Funplay Cocos MCP 0.6.3，采用上游 MIT 许可代码作为底座。
- 必须保留 `LICENSE` 中的 `Copyright (c) 2026 Funplay`、完整 MIT 授权条款和免责声明。分发源码或包含上游代码的安装包时，必须随包保留这些声明。
- 修改 README、打包配置或发布流程时，不得删除或隐藏上游归属和许可说明。

## 禁止混入限制商用的代码

- **严禁复制、移植、改写后引入或合并 `../cocos-mcp-server-main` 中的代码及其衍生代码。**该项目的本地 README 明确限制商用和转售，不属于本项目可使用的代码来源。
- 禁令覆盖源代码、编译产物、脚本、模板、样式、资源以及代码片段；不得通过重命名、转换语言或让其他代理改写来规避。
- 实现类似功能时，依据公开功能需求、Cocos 官方文档、公开 API 及许可兼容的材料独立设计和编写。不得根据受限制源码逐行或逐段翻译实现。
- 不得复制 VberAI Pro 的非公开代码，不得实现付费插件授权绕过，也不得冒用 Funplay 或 VberAI 的品牌、标识或官方身份。
- `../cocos-mcp-v1.8.1-all` 是官网商业下载包，仅作功能分析对象，不是可复用来源。不得引入其中的代码、混淆产物、加密载荷、模板、知识库正文或资源；不得根据其实现翻译、改写或解密复刻。
- 引入第三方代码或资源前，核查来源、许可证和商用条件；来源或许可不明确时不得纳入项目。

## 开发与验证

- 当前目录是 Cocos Creator 扩展项目，不是完整游戏工程。项目使用独立的 `cocos-mcp-kit` 包、扩展、面板与配置标识；Funplay 原版权仍须保留。
- 本项目面向开源发布，不规划账号认证、用户登录或登录页模板；游戏 UI 模板以暂停菜单、设置和结果界面为首批目标。
- 在扩展现有功能时优先复用本项目中的 Funplay 实现，新增高频操作应提供清晰的参数校验和错误处理。
- 变更功能后运行相关测试；涉及场景、组件、资源或预制体时，还需在目标 Cocos Creator 版本中验证实际保存、重新打开及引用持久化，不能仅以 `success: true` 判断成功。
- 未配置本项目自有发布渠道前，不得使用上游仓库作为自动更新或全局安装来源；发布前须核查包名、仓库、注册表、安装目标和完整许可声明。
