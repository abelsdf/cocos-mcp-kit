# 普通场景节点移动验证（2026-09-19）

测试工程：`D:\AI\Game\arrow-puzzle`，Cocos Creator 3.8.8，工程身份 `758048aa923da9e57466f98a`，本地 MCP 端口 `21482`。运行中的扩展尚未安装本轮源码；通过其 `execute_scene_script` 在场景进程加载本工作区的 `scene.js` 并调用当前 `moveNode`。工具注册由本工作区单元测试验证，旧扩展面板尚未暴露此新工具。

| 步骤 | 验证结果 |
| --- | --- |
| 隔离样本 | `asset-db:create-asset` 创建并打开 `McpKitNodeMoveProbe.scene`，资产 UUID `f75cfef2-dac5-44c1-96c6-00221ec6f927`。`MoveA` 局部 x=100，`MoveB` 局部 x=300，`MoveChild` 起初在 A 下、局部 x=25，世界 x=125。 |
| 默认移动 | 将子节点移到 B 下，`keepWorldTransform` 默认 `true`；世界 x 仍为 125，局部 x 变为 -175，返回的新路径为 `MoveB/MoveChild`。 |
| 局部模式 | 移回 A 并设置 `keepWorldTransform:false`；局部 x 仍为 -175，世界 x 为 -75。 |
| 错误保护 | 尝试把 A 移到其后代 `MoveChild` 下，返回循环层级错误，A 仍位于场景根。单元测试另覆盖重名定位、场景根、非法布尔值、同父节点幂等及关联预制体拒绝。 |
| 持久化 | 最后再将子节点移到 B 下，显式执行 `scene:save-scene`，切至 `ComplexRefs.scene`，再打开探针。子节点 UUID `7c6eEqcDtACbwTW5qxXUUj` 仍在 B 下，局部 x=-375、世界 x=-75；磁盘场景文件与 `.meta` 均存在。 |
| 清理 | 切回 `ComplexRefs.scene`，用 `asset-db:delete-asset` 删除探针；资产查询为空，场景文件及 `.meta` 均不存在。 |

本轮验证普通场景节点的父子关系和位置持久化。关联预制体实例移动被工具主动拒绝，避免绕过 Creator 的实例覆盖记录；同级排序及复杂旋转、缩放组合尚未验收。

本工作区 `node --test` 全量 290 项通过、0 失败、0 跳过；工具文档生成一致性检查通过。
